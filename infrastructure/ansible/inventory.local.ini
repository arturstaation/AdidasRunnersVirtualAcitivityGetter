# Control node = a própria VM (rode o playbook clonando o repo na VM e
# executando daqui). Mesmo modelo do inventory.local.ini da Florae.
[adidas]
localhost ansible_connection=local ansible_user=opc
